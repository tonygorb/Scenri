{
  "name": "scenri-monorepo",
  "private": true,
  "version": "0.4.5",
  "license": "AGPL-3.0-only",
  "author": "Tony Gorb <hello@scenri.co>",
  "packageManager": "pnpm@9.15.0",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "test": "pnpm -r --no-bail test",
    "build": "pnpm --filter @scenri/studio build",
    "dev": "tsx packages/cli/src/index.ts",
    "typecheck": "pnpm -r --no-bail typecheck",
    "verify": "pnpm typecheck && pnpm lint && pnpm test",
    "verify:studio": "pnpm --filter @scenri/studio typecheck && pnpm exec biome lint apps/studio/src && pnpm --filter @scenri/studio test",
    "lint": "biome lint .",
    "format": "biome format --write .",
    "check": "biome check .",
    "ci": "biome ci .",
    "test:visual": "pnpm --filter @scenri/studio exec playwright test -c playwright.visual.config.ts"
  },
  "pnpm": {
    "overrides": {
      "tsup>esbuild": ">=0.28.1"
    }
  },
  "devDependencies": {
    "@biomejs/biome": "^2.5.9",
    "@types/node": "^22.0.0",
    "iconv-lite": "0.6.3",
    "tsup": "^8.5.1",
    "tsx": "^4.23.12",
    "typescript": "^5.5.0",
    "vitest": "^3.0.0"
  }
}
