node_modules/
dist/
build/
out/
*.tgz

# assembled by packages/cli/scripts/prepack.mjs at publish time, never committed
packages/cli/studio-dist/
packages/cli/templates/
packages/cli/LICENSE
packages/cli/NOTICE
packages/cli/README.md
.pnpm-store/
.DS_Store
*.local
coverage/
*.log

# secrets. .env* is broad on purpose: it catches .env.local, .env.production
# and anything else someone reaches for in a hurry.
.env*
*.pem
*.key
*.p12
*.pfx

# agent and editor tooling. These write into the repo and have already leaked
# an absolute home path once, so they stay out.
# ...except the skills, which are project doctrine rather than machine state:
# the creative system's rules, the engine behaviours we paid quota to learn, and
# the casting board. A clone without them cannot do this work. Written with
# repo-relative paths only — never absolute — which is what the leak was about.
# Note the trailing /*: git cannot re-include anything under an excluded
# DIRECTORY, so the contents are excluded and the one keeper is negated.
.claude/*
!.claude/skills/
.serena/
.cursor/
.remember/
.aider*

# playwright
apps/studio/test-results/
apps/studio/playwright-report/
.playwright-mcp/

# design mockups. Tony sketches new pages and components as static HTML here
# before building them for real. That is a personal pre-code step, so it lives
# in a private working repo and this path is a symlink to it. No trailing
# slash: git sees a symlink, not a directory, and a trailing slash would miss it.
docs/design

# local research notes — keep off GitHub
docs/research/
docs/product/
