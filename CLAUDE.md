@AGENTS.md

Claude Code reads this file in every checkout, so the conduct above reaches a session in a linked
worktree as well as in the primary. Everything Claude-specific and private lives in
`.claude/CLAUDE.md`, which is local to the machine and loaded alongside this file.
